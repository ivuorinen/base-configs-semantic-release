---
# zizmor configuration: https://docs.zizmor.sh/configuration/
#
# No rule suppressions. The two that used to live here are both resolved rather
# than ignored: `adhoc-packages` (the workflows now use the Corepack the runner
# provides instead of `npm install -g corepack`) and `use-trusted-publishing`
# (publish.yml authenticates to npm via OIDC and carries no NPM_TOKEN).
#
# Re-add a suppression only with the reason written next to it.
rules: {}
