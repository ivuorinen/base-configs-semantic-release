# Dependencies
node_modules/
.yarn/cache
.yarn/unplugged
.yarn/build-state.yml
.yarn/install-state.gz
.pnp.*

# Build and test output
coverage/
*.tgz
node-compile-cache

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json

# Caches
.eslintcache
.npm
.cache/

# Local environment. `.env*` rather than `.env` + `.env*.local`, which left
# .env.production and .env.development tracked. Example files are opted back in.
.env*
!.env.example
!.env*.example

# Editors and OS
.DS_Store
.idea/
*~
